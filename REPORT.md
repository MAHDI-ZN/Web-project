# گزارش نهایی پروژه Melody

درس برنامه‌سازی وب — دانشگاه صنعتی شریف  
فاز اول: فرانت‌اند (Next.js) · فاز دوم: بک‌اند (Django REST) و ادغام

---

## ۱. نقش و کارهای هر عضو در هر فاز

این گزارش برای تحویل گروهی است؛ نقش‌ها را مطابق تقسیم واقعی گروه تکمیل کنید.

| عضو | فاز ۱ (فرانت) | فاز ۲ (بک‌اند و ادغام) |
|------|----------------|-------------------------|
| عضو ۱ | صفحات ورود/ثبت‌نام، خانه، نمایه، تنظیمات | مدل User، احراز هویت JWT، تنظیمات کاربر |
| عضو ۲ | پخش‌کننده، پلی‌لیست، آلبوم/تک‌آهنگ، پنل هنرمند | کاتالوگ، آپلود فایل، استریم، پیشنهاددهنده |
| عضو ۳ | داشبورد پشتیبان/مدیر، اعلان‌ها، PWA | پرداخت، گزارش‌های تجمیعی، دسترسی‌ها، Docker |

فعالیت همه اعضا در هر دو فاز مطابق صورت‌پروژه الزامی است.

---

## ۲. قوانین توسعه و فرآیند

- **نام‌گذاری:** Python `snake_case` برای ماژول/مدل/فیلد؛ TypeScript `camelCase` برای API از طریق `djangorestframework-camel-case`.
- **کلاس‌ها:** مدل‌های دامنه در اپ‌های جدا (`accounts`, `catalog`, `social`, `billing`, `reports`).
- **شاخه‌ها (پیشنهادی):** `main` برای تحویل، `feat/...` برای ویژگی، `fix/...` برای باگ.
- **REST:** فقط متدهایی که معنا دارند. رویداد استریم فقط `POST` است و `PUT` ندارد. قیمت اشتراک به‌صورت منبع تکی با `GET`/`PUT` به‌روز می‌شود.
- **رمز عبور:** هرگز در پاسخ API برنمی‌گردد؛ هش Django.
- **تست:** Vitest در فرانت، pytest-django در بک‌اند.

---

## ۳. ساختار دو پروژه و توجیه

```
melody/
  src/                 فرانت Next.js App Router
  backend/
    config/            تنظیمات و URLهای ریشه
    core/              مجوز، سیاست اشتراک، احراز هویت
    accounts/          کاربر، فالو، هنرمند
    catalog/           آهنگ، آلبوم، پلی‌لیست، استریم
    social/            اعلان و تیکت
    billing/           قیمت، پرداخت، تسویه
    reports/           گزارش‌های تجمیعی
  docker-compose.yml
```

جداسازی فرانت و بک‌اند مطابق صورت‌پروژه (Next.js + Django) است. لایه `src/lib/api` قرارداد REST را از UI جدا می‌کند تا صفحهها به جزئیات HTTP وابسته نباشند.

---

## ۴. توجیه نگهداری‌پذیری

- منطق سقف اشتراک در **Strategy** (`core/policies.py`) است؛ افزودن سطح جدید بدون پخش `if/else` در ویوها انجام می‌شود.
- درگاه پرداخت با **Adapter** عوض می‌شود (`mock` یا `zarinpal`) بدون تغییر ویو.
- اعلان‌ها با **Factory** ساخته می‌شوند تا متن/مخاطب در یک جا بماند.
- گزارش‌ها در سرویس بک‌اند جمع می‌شوند؛ فرانت فقط عدد آماده را نشان می‌دهد.
- ViewSetهای DRF و Serializerها از تکرار CRUD جلوگیری می‌کنند.

---

## ۵. مدل‌های بک‌اند، روابط و توجیه وجود

| مدل | نقش | روابط |
|------|------|--------|
| `User` | هویت، نقش، اشتراک، تنظیمات همگام | پایه سیستم |
| `ArtistProfile` | وضعیت تأیید هنرمند | OneToOne با User |
| `Follow` | دنبال‌کردن | M2M صریح بین کاربران |
| `Album` / `Track` | کاتالوگ | Track به Album؛ هنرمندان M2M |
| `Playlist` + `PlaylistTrack` | لیست پخش با ترتیب | owner و tracks |
| `StreamEvent` | رویداد پخش (فقط ایجاد) | برای شنونده یکتا و گزارش |
| `Notification` | اعلان نقش‌محور | FK به User |
| `Ticket` / `TicketMessage` | پشتیبانی | چت‌باکس تیکت |
| `SubscriptionPrice` | قیمت پویا (تک‌ردیفی) | بدون نیاز به تغییر کد |
| `Payment` | تراکنش اشتراک | وضعیت pending/success/failed |
| `ArtistPayout` | پاداش ماهانه | unique listeners + streams |

مدل اضافه برای «لاگ خام کاربران در گزارش» وجود ندارد؛ گزارش‌ها `Count`/`Sum` می‌گیرند.

**فرمول پاداش:**  
`amount = unique_listeners × 500 + streams × 50` تومان  
نرخ‌ها از تنظیمات قابل تغییرند (`ROYALTY_LISTENER_RATE`, `ROYALTY_STREAM_RATE`).

---

## ۶. نقش هوش مصنوعی در توسعه

از دستیار کدنویسی برای اسکلت Django/DRF، اتصال فرانت به API، Docker و پیش‌نویس این گزارش استفاده شد. تصمیم‌های دامنه (سقف اشتراک، REST، فرمول پاداش، Adapter پرداخت) توسط تیم بازبینی و در کد تثبیت شد. تست‌های بک‌اند برای جلوگیری از پذیرش کد تولیدشدهٔ نادرست نوشته شدند.

---

## ۷. نمونه کد هوش مصنوعی در هر فاز

**فاز ۱ (فرانت):** تولید اولیه کارت آهنگ و استور Zustand با Local Storage.

**فاز ۲ (بک‌اند):** اسکلت `TrackViewSet` و Serializerها؛ سپس محدودیت early-access و استریم روزانه دستی اصلاح شد.

نمونه الگوی نهایی (نه خروجی خام) برای سیاست اشتراک:

```python
class SubscriptionPolicyFactory:
    @staticmethod
    def for_tier(tier: str) -> SubscriptionPolicy:
        return _POLICIES.get(tier, _POLICIES["basic"])
```

---

## ۸. ضعف و قوت هوش مصنوعی

**قوت:** سرعت راه‌اندازی پروژه، یکنواختی CRUD با DRF، پیش‌نویس Docker و مستند.  
**ضعف:** تمایل به PUT اضافه، محاسبه گزارش روی فرانت، و جزئیات multipart/camelCase که باید دستی درست می‌شد. در فرانت گاهی state ماک را با API قاطی می‌کرد.

---

## ۹. چک‌لیست فاز ۲

- [x] طراحی صحیح مدل‌ها
- [x] رعایت قواعد REST
- [x] اندپوینت CRUD به‌اندازه
- [x] ارسال و دریافت فایل
- [x] ادغام با فرانت و کارکرد هر دو بخش
- [x] حداقل ۱۵ تست بک‌اند
- [x] اصول Django/DRF و پرهیز از کد تکراری
- [x] الگوهای طراحی (Strategy, Adapter, Factory)
- [x] کد تمیز و تغییرپذیر
- [x] داکرایز (امتیازی)
- [x] سیستم پیشنهاددهنده آهنگ (امتیازی انتخابی)

---

## ۱۰. اجرای تحویل

```bash
docker compose up --build
# یا: بک‌اند روی 8000 و فرانت روی 3000 به‌صورت محلی
```

ورود با `nima@demo.com` / `demo1234` برای مشاهده دسترسی زودهنگام و آمار.
